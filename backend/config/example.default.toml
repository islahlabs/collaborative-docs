# Default configuration for collaborative-docs backend

[server]
host = "0.0.0.0"
port = 3000

[database]
host = "localhost"
port = 5432
username = "postgres"
password = "password"
database = "collaborative_docs"
max_connections = 10
min_connections = 2

[cors]
allowed_origins = [
    "http://localhost:5173",
    "http://localhost:3000"
]
allowed_methods = [
    "GET",
    "POST", 
    "PUT",
    "DELETE"
] 